flutter build web --release --dart-define=BACKEND_URL=https://prodbuilderex-production.up.railway.app

# After building, cd build/web
# and then serve it with any web server.
# For example:
# python3 -m http.server 8080